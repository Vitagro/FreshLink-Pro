{
  "buildCommand": "pnpm run typecheck && pnpm --filter \"./artifacts/freshlink\" --filter \"./artifacts/api-server\" run build",
  "installCommand": "pnpm install",
  "outputDirectory": "artifacts/freshlink/dist/public",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
