From 511a194996d9f76526c276c550817a915a4cf9b6 Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Thu, 6 Aug 2026 23:58:05 +0000
Subject: [PATCH] Add truck icon toggle for IMPRESSION GROUPEE and preparateur assignment dropdown

---
 artifacts/freshlink/src/components/backoffice/BOBonLivraison.tsx  | 174 +++++++++---------
 artifacts/freshlink/src/components/mobile/MobilePreparation.tsx   |  36 +++-
 2 files changed, 121 insertions(+), 89 deletions(-)

diff --git a/artifacts/freshlink/src/components/backoffice/BOBonLivraison.tsx b/artifacts/freshlink/src/components/backoffice/BOBonLivraison.tsx
index 08e503c..c818c80 100644
--- a/artifacts/freshlink/src/components/backoffice/BOBonLivraison.tsx
+++ b/artifacts/freshlink/src/components/backoffice/BOBonLivraison.tsx
@@ -997,6 +997,7 @@ export default function BOBonLivraison({ user }: { user: User }) {
   // ── Print multiple BL selection ────────────────────────────────────────────
   const [selectedPrint, setSelectedPrint] = useState<Set<string>>(new Set())
   const [filterSecteur, setFilterSecteur] = useState("")
+  const [showPrintGrouped, setShowPrintGrouped] = useState(true)
   const togglePrintSel = (blId: string) => {
     const newSet = new Set(selectedPrint)
     if (newSet.has(blId)) newSet.delete(blId)
@@ -1728,103 +1729,103 @@ export default function BOBonLivraison({ user }: { user: User }) {
             <div className="flex gap-2 items-center">
               <span className="text-xs font-bold text-blue-700 bg-white px-3 py-1.5 rounded-full shadow-sm">{selectedPrint.size} sélectionné{selectedPrint.size !== 1 ? "s" : ""}</span>
               <span className="text-xs font-bold text-white bg-blue-600 px-3 py-1.5 rounded-full">{displayed.length} disponible</span>
+              <button onClick={() => setShowPrintGrouped(v => !v)} className="text-2xl hover:scale-110 transition-transform" title={showPrintGrouped ? "Masquer" : "Afficher"}>🚚</button>
             </div>
             </div>
 
-            {/* Groupement rapide par livreur/secteur */}
-            <div className="mb-4 p-4 bg-white rounded-xl border border-blue-200">
+            {showPrintGrouped && (
+              <>
+                {/* Groupement rapide par livreur/secteur */}
+                <div className="mb-4 p-4 bg-white rounded-xl border border-blue-200">

diff --git a/artifacts/freshlink/src/components/mobile/MobilePreparation.tsx b/artifacts/freshlink/src/components/mobile/MobilePreparation.tsx
index 96cfcb4..854a9a5 100644
--- a/artifacts/freshlink/src/components/mobile/MobilePreparation.tsx
+++ b/artifacts/freshlink/src/components/mobile/MobilePreparation.tsx
@@ -164,6 +164,7 @@ export default function MobilePreparation({ user }: Props) {
   const [clientSortMode, setClientSortMode] = useState<ClientSortMode>("ordre")
   const [articleSortMode, setArticleSortMode] = useState<"nom" | "famille">("nom")
   const [localPreparedByClient, setLocalPreparedByClient] = useState<Record<string, string>>({})
+  const [showPreparateurs, setShowPreparateurs] = useState(false)
 
   const contenantGros = contenants.find(c => c.nom.toLowerCase().includes("gros") || c.nom.toLowerCase().includes("plastique"))
   const contenantDemi = contenants.find(c => c.nom.toLowerCase().includes("demi") || c.nom.toLowerCase().includes("petit"))
@@ -205,6 +206,17 @@ export default function MobilePreparation({ user }: Props) {
     setClientViewId(null)
   }
 
+  const handleAssignPreparateur = (bonId: string, preparateurId: string) => {
+    const updated = bons.map(b =>
+      b.id === bonId ? { ...b, preparateurId: preparateurId || undefined } : b
+    )
+    setBons(updated)
+    store.saveBonsPreparation(updated)
+    if (activeBon?.id === bonId) {
+      setActiveBon(activeBon ? { ...activeBon, preparateurId: preparateurId || undefined } : null)
+    }
+  }
+
   const validateLigne = (articleId: string) => {
     if (!activeBon) return
     const arr = store.getBonsPreparation()
@@ -812,11 +824,25 @@ export default function MobilePreparation({ user }: Props) {
             const total = bon.lignes.length
             const progress = total > 0 ? Math.round((validated / total) * 100) : 0
             return (
-              <button key={bon.id} onClick={() => openBon(bon)}
+              <div key={bon.id}
                 className="bg-card rounded-2xl border border-border p-4 text-left hover:shadow-sm transition-shadow w-full">
-                <div className="flex items-center justify-between mb-2">
-                  <p className="font-bold text-foreground">{bon.nom}</p>
+                <div className="flex items-center justify-between gap-3 mb-2 flex-wrap">
+                  <button onClick={() => openBon(bon)} className="flex-1 text-left hover:opacity-70">
+                    <p className="font-bold text-foreground">{bon.nom}</p>
+                  </button>
                   <StatusBadge s={bon.statut} />
+                  <select
+                    onClick={(e) => e.stopPropagation()}
+                    onChange={(e) => handleAssignPreparateur(bon.id, e.target.value)}
+                    value={bon.preparateurId || ""}
+                    className="px-2 py-1 text-xs border border-border rounded-lg bg-card focus:outline-none focus:ring-2 focus:ring-blue-500/30">
+                    <option value="">Non assigné</option>
+                    {store.getUsers()
+                      .filter(u => u.role === "preparateur" || u.role === "magasinier")
+                      .map(u => (
+                        <option key={u.id} value={u.id}>{u.name}</option>
+                      ))}
+                  </select>
                 </div>
                 <p className="text-xs text-muted-foreground mb-3">
                   {bon.date} — {total} articles — {bon.lignes.reduce((s, l) => s + l.qteCommandee, 0).toFixed(1)} kg
@@ -819,10 +845,10 @@ export default function MobilePreparation({ user }: Props) {
                   </div>
                   <span className="text-xs font-bold text-foreground shrink-0">{progress}%</span>
                 </div>
-                <p className="text-xs text-muted-foreground mt-2">
+                <p className="text-xs text-muted-foreground mt-2" onClick={() => openBon(bon)}>
                   {validated}/{total} articles validés
                 </p>
-              </button>
+              </div>
             )
           })}
         </div>
--
2.43.0